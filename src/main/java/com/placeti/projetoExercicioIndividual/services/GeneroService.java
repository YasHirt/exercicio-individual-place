package com.placeti.projetoExercicioIndividual.services;

// REVISÃO: GeneroService está vazio e não tem a anotação @Service.
// Sem @Service, o Spring não vai registrar essa classe como bean e não será possível injetá-la.
// Implemente os métodos para criar (POST) e listar (GET) gêneros, conforme o exercício exige.
// Siga o mesmo padrão do FilmeService: injeção via construtor, uso do repository, retorno de DTO.
public class GeneroService {
}
